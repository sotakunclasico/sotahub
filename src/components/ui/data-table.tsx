export function DataTable({ columns, rows }: { columns: string[]; rows: Array<Array<string | number>> }) { return <div className="overflow-x-auto rounded-2xl border border-white/10"><table className="w-full min-w-140 text-left text-sm"><thead className="bg-white/[.04] text-xs uppercase tracking-widest text-slate-500"><tr>{columns.map(c=><th className="px-5 py-4" key={c}>{c}</th>)}</tr></thead><tbody className="divide-y divide-white/[.06]">{rows.map((row,i)=><tr className="text-slate-300 hover:bg-white/[.025]" key={i}>{row.map((cell,j)=><td className="px-5 py-4" key={j}>{cell}</td>)}</tr>)}</tbody></table></div>; }
